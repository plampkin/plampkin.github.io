source "https://rubygems.org"

# Matches the exact Jekyll + plugin versions GitHub Pages builds with, so what
# you see locally is what you get when deployed.
gem "github-pages", group: :jekyll_plugins

# Ruby 3.0+ no longer ships webrick, which `jekyll serve` needs locally.
gem "webrick", "~> 1.8"
